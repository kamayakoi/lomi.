# Dependencies (legacy TS CLI artifacts)
node_modules/

# Rust build output
/target/

# OS / editor
.DS_Store

# Environment
.env
.env.*

# Legacy TS build output
dist/

# rules/api-reference.md is committed for offline builds (refresh via scripts/generate-rules.sh)
