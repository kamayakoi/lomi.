---
title: 'Configuration'
description: 'Global CLI config, project environment variables, profiles, and CI settings for the lomi. CLI.'
---

The lomi. CLI uses two layers of configuration: **global** (authentication and profiles) and **project-level** (SDK keys and app settings).

## Global CLI config

Stored after [`lomi login`](/core/lomi-cli/auth):

| Platform | Path |
| --- | --- |
| macOS | `~/Library/Application Support/lomi/config.json` |
| Linux | `~/.config/lomi/config.json` |

Override with:

```bash filename="Terminal"
export LOMI_CONFIG_DIR=/custom/path/lomi
```

### Config file shape

```json filename="config.json"
{
  "version": 2,
  "current_profile": "default",
  "profiles": {
    "default": {
      "cli_token": "lomi_sk_...",
      "api_url": "https://api.lomi.africa"
    },
    "sandbox": {
      "cli_token": "lomi_sk_test_...",
      "api_url": "https://sandbox.api.lomi.africa"
    }
  },
  "settings": {
    "has_seen_rules_install_prompt": true,
    "last_rules_install_version": "1.0.0"
  }
}
```

Manage profiles with [`lomi list-profiles`](/core/lomi-cli/commands#lomi-list-profiles), [`lomi switch`](/core/lomi-cli/commands#lomi-switch-profile), and [`lomi logout`](/core/lomi-cli/commands#lomi-logout).

## Environment variables

### CLI authentication

| Variable | Description |
| --- | --- |
| `LOMI_ACCESS_TOKEN` | CLI token for CI/headless use (overrides stored profile token) |
| `LOMI_CONFIG_DIR` | Override global config directory |
| `LOMI_SUPABASE_ANON_KEY` | Override Supabase anon key for device auth (optional) |

### Project SDK (via `.env`)

Set by [`lomi init`](/core/lomi-cli/init) in your project root:

| Variable | Description |
| --- | --- |
| `LOMI_API_KEY` | Secret API key for the SDK (`lomi_sk_test_…` / `lomi_sk_live_…`) |
| `LOMI_WEBHOOK_SECRET` | Webhook signing secret for signature verification |
| `LOMI_API_URL` | API base URL (`https://api.lomi.africa` or sandbox) |

<Callout type="warning" emoji="⚠️" title="Two credential types">
  **`LOMI_ACCESS_TOKEN`** — authenticates the **CLI** (from `lomi login`).

  **`LOMI_API_KEY`** — authenticates your **application SDK** (from the Merchant Portal, stored in `.env` by `lomi init`).
</Callout>

## Project config file

`lomi init` creates `lomi.config.ts` for project-level settings used by examples and tooling.

## Global CLI flags

Override config per command:

```bash filename="Terminal"
lomi --profile sandbox status
lomi -a https://sandbox.api.lomi.africa checkout create
lomi --log-level debug dev
```

| Flag | Description |
| --- | --- |
| `--profile` | Profile name (default: `default`) |
| `-a, --api-url` | API base URL override |
| `-l, --log-level` | Log verbosity |
| `--skip-telemetry` | Opt out of telemetry |

## API URLs

| Environment | URL |
| --- | --- |
| Production | `https://api.lomi.africa` |
| Sandbox | `https://sandbox.api.lomi.africa` |

Profiles `sandbox` and `default` map to the appropriate URL automatically during login.

## CI example

```bash filename="Terminal"
export LOMI_ACCESS_TOKEN="${{ secrets.LOMI_CLI_TOKEN }}"
export LOMI_API_KEY="${{ secrets.LOMI_API_KEY }}"

lomi status
lomi init --yes \
  --environment sandbox \
  --language ts \
  --api-key "$LOMI_API_KEY" \
  --skip-package-install \
  --skip-rules-install
```

## Next steps

- [Authenticate](/core/lomi-cli/auth)
- [Initialize a project](/core/lomi-cli/init)
- [Authentication reference](/reference/setup/authentication)
