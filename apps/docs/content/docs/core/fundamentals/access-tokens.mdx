---
title: 'Environment variables'
description: 'Securely manage your API keys and environment variables.'
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## API keys

lomi. provides two types of API keys for each environment:

| Key | Prefix (current) | Where to use |
| --- | --- | --- |
| **Secret** | `lomi_sk_test_…` / `lomi_sk_live_…` | Servers, scripts, CI — full API access. **Never expose in browsers or mobile apps.** |
| **Publishable** | `lomi_pk_test_…` / `lomi_pk_live_…` | Client-side card confirmation, Payment Elements — limited operations only. |

<Callout type="warn">
  **Never put secret keys in frontend code.** Use publishable keys only where the integration guide explicitly allows it (e.g. embedded card flows).
</Callout>

<Callout type="info">
  Older examples may show `sk_test_` / `sk_live_` prefixes. Current keys issued by the dashboard follow **`lomi_sk_test_`** and **`lomi_sk_live_`** as in [Authentication](/reference/setup/authentication).
</Callout>

## Managing environment variables

Create a `.env` file at your project root (add `.env` to `.gitignore`). Use **test** keys while integrating against the sandbox API (`https://sandbox.api.lomi.africa`).

```bash filename=".env.example"
# Sandbox (recommended while building)
LOMI_SECRET_KEY=lomi_sk_test_xxxxxxxxxxxxxxxxxxxxxx
LOMI_PUBLISHABLE_KEY=lomi_pk_test_xxxxxxxxxxxxxxxxxxxxxx

# Webhook signing secret (from Developers → Webhooks → your endpoint)
LOMI_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxx

# Optional: only if you call the API without the SDK and need an explicit host
# LOMI_API_URL=https://sandbox.api.lomi.africa
```

Copy to `.env` and replace placeholders with values from **Settings → Access tokens** and your webhook endpoint settings. Load `.env` with your framework (`dotenv`, Next.js, Vite, etc.) or your process manager — do not commit real keys.

## Initialization examples

How to initialize the SDK with your environment variables:

<Tabs items={["TypeScript", "Python", "Go", "PHP"]}>
<Tab value="TypeScript">
```typescript
import { LomiSDK } from '@lomi./sdk';

const lomi = new LomiSDK({
apiKey: process.env.LOMI_SECRET_KEY!,
environment: 'live', // or 'test'
// Optional: Custom timeout
timeout: 30000,
});

````
</Tab>
<Tab value="Python">
```python
import os
from lomi import LomiClient

api_client = LomiClient(
    api_key=os.environ["LOMI_SECRET_KEY"],
    environment="test" # or "live"
)
````

</Tab>
<Tab value="Go">
```go
import (
    "os"
    lomi "github.com/lomiafrica/lomi-go-sdk"
)

configuration := lomi.NewConfiguration()

// Override host for sandbox
configuration.Servers = lomi.ServerConfigurations{
{
URL: "https://sandbox.api.lomi.africa",
Description: "Sandbox Environment",
},
}

client := lomi.NewAPIClient(configuration)

````
</Tab>
<Tab value="PHP">
```php
use Lomi\Configuration;

$config = Configuration::getDefaultConfiguration()
    ->setApiKey('X-API-KEY', getenv('LOMI_SECRET_KEY'))
    ->setHost('https://sandbox.api.lomi.africa');
````

</Tab>
</Tabs>

## Webhook verification

Verify incoming webhooks using your `LOMI_WEBHOOK_SECRET`:

<Tabs items={["TypeScript", "Python", "Go", "PHP"]}>
<Tab value="TypeScript">
```typescript
// Express example
import crypto from 'crypto';

app.post('/webhook', (req, res) => {
const signature = req.headers['x-lomi-signature'];
const secret = process.env.LOMI_WEBHOOK_SECRET!;

const expected = crypto
.createHmac('sha256', secret)
.update(req.rawBody)
.digest('hex');

if (signature !== expected) {
return res.status(400).send('Invalid signature');
}

// Process event...
});

````
</Tab>
<Tab value="Python">
```python
# Flask example
import hmac
import hashlib

@app.route('/webhook', methods=['POST'])
def webhook():
    signature = request.headers.get('x-lomi-signature')
    secret = os.environ['LOMI_WEBHOOK_SECRET'].encode()

    expected = hmac.new(secret, request.data, hashlib.sha256).hexdigest()

    if not hmac.compare_digest(signature, expected):
        return jsonify({'error': 'Invalid signature'}), 400

    # Process event...
````

</Tab>
<Tab value="Go">
```go
func webhookHandler(w http.ResponseWriter, r *http.Request) {
    signature := r.Header.Get("x-lomi-signature")
    secret := os.Getenv("LOMI_WEBHOOK_SECRET")
    
    body, _ := io.ReadAll(r.Body)
    mac := hmac.New(sha256.New, []byte(secret))
    mac.Write(body)
    expected := hex.EncodeToString(mac.Sum(nil))
    
    if !hmac.Equal([]byte(signature), []byte(expected)) {
        http.Error(w, "Invalid signature", http.StatusBadRequest)
        return
    }
}
```
</Tab>
<Tab value="PHP">
```php
$signature = $_SERVER['HTTP_X_LOMI_SIGNATURE'];
$secret = getenv('LOMI_WEBHOOK_SECRET');
$payload = file_get_contents('php://input');

$expected = hash_hmac('sha256', $payload, $secret);

if (!hash_equals($expected, $signature)) {
die('Invalid signature');
}

```
</Tab>
</Tabs>

