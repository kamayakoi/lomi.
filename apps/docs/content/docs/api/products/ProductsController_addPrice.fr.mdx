---
title: "Ajouter un prix à un produit"
description: "Ajouter un prix à un produit"
full: true
method: post
path: /products/{id}/prices
operationId: ProductsController_addPrice
---

## Aperçu

Ajouter un prix à un produit

Ajoute une nouvelle option tarifaire à un produit (palier, devise ou période). Un produit ne peut avoir au plus 3 prix actifs. Les prix existants ne se modifient pas : créez-en un nouveau.


### Quand utiliser cet endpoint

Utilisez cet endpoint quand vous devez exécuter `POST /products/&#123;id&#125;/prices` (Ajouter un prix à un produit).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Aperçu de l’intégration](/reference/setup/overview)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Point de terminaison

`POST /products/{id}/prices`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres de chemin

| Nom | Obligatoire | Schéma | Description |
| --- | --- | --- | --- |
| `id` | Oui | — | UUID du produit |

### Paramètres de requête

_Aucun paramètre de requête._


### Corps de la requête

_Aucun corps application/json pour cette opération._


## Réponses

| Statut | Description |
| --- | --- |
| `201` | Prix ajouté avec succès |
| `400` | Entrée invalide ou nombre maximal de prix dépassé |
| `401` | Clé API invalide ou manquante |
| `404` | Produit introuvable ou accès refusé |

## Erreurs

Les erreurs suivent le format JSON standard (code de statut et message lisible par machine). Validez les entrées avant appel ; **401** indique une clé manquante/invalide, **404** une ressource introuvable pour cette organisation, **429** une limitation de débit. Pour les retries sûrs sur les créations, envoyez une clé d’idempotence quand votre flux le permet.

## Exemple

```bash
curl -sS -X POST "https://sandbox.api.lomi.africa/products/value/prices" \
  -H "X-API-KEY: $LOMI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

## OpenAPI

- **operationId**: `ProductsController_addPrice`
- **Operation**: `POST /products/{id}/prices`

Schémas complets et **Try it** : [référence API](/api/index). Contrat machine : `openapi.json` à la racine des docs.
