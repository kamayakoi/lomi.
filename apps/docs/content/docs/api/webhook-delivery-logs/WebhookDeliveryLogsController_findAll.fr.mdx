---
title: "Lister les journaux de livraison"
description: "Lister les journaux de livraison"
full: true
method: get
path: /webhook-delivery-logs
operationId: WebhookDeliveryLogsController_findAll
---

## Aperçu

Lister les journaux de livraison

Renvoie les journaux de livraison pour un webhook donné. Ils sont créés automatiquement lors des tentatives d'envoi. Filtrez avec le paramètre de requête webhookId.


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Aperçu de l’intégration](/reference/setup/overview)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Point de terminaison

`GET /webhook-delivery-logs`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres de chemin

_Aucun paramètre de chemin au-delà du motif d’URL._


### Paramètres de requête

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `offset` | query | Non | — | Nombre de journaux à ignorer (pagination) |
| `limit` | query | Non | — | Nombre maximal de journaux |
| `failedOnly` | query | Non | — | Uniquement les livraisons en échec |
| `successOnly` | query | Non | — | Uniquement les livraisons réussies |
| `webhookId` | query | Oui | — | Filtrer par identifiant de webhook |



## Réponses

| Statut | Description |
| --- | --- |
| `200` | Liste des journaux de livraison |
| `401` | Clé API invalide ou manquante |

## Erreurs

Les erreurs suivent le format JSON standard (code de statut et message lisible par machine). Validez les entrées avant appel ; **401** indique une clé manquante/invalide, **404** une ressource introuvable pour cette organisation, **429** une limitation de débit. Pour les retries sûrs sur les créations, envoyez une clé d’idempotence quand votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/webhook-delivery-logs?offset=1&limit=1" \
  -H "X-API-KEY: $LOMI_API_KEY"
```

## OpenAPI

- **operationId**: `WebhookDeliveryLogsController_findAll`
- **Operation**: `GET /webhook-delivery-logs`

Schémas complets et **Try it** : [référence API](/api/index). Contrat machine : `openapi.json` à la racine des docs.
