---
title: "Lister les paiements vers bénéficiaires"
description: "Lister les paiements vers bénéficiaires"
full: true
method: get
path: /beneficiary-payouts
operationId: BeneficiaryPayoutsController_findAll
---

## Aperçu

Lister les paiements vers bénéficiaires

Renvoie les paiements de l'organisation avec pagination et filtres optionnels (statuts, plage de dates, devise).


### Quand utiliser cet endpoint

Utilisez cet endpoint quand vous devez exécuter `GET /beneficiary-payouts` (Lister les paiements vers bénéficiaires).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Aperçu de l’intégration](/reference/setup/overview)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Point de terminaison

`GET /beneficiary-payouts`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres de chemin

_Aucun paramètre de chemin au-delà du motif d’URL._


### Paramètres de requête

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `offset` | query | Non | — | Décalage pour la pagination |
| `limit` | query | Non | — | Nombre maximal de résultats |
| `currencyCode` | query | Non | — | Code devise |
| `endDate` | query | Non | — | Date de fin (ISO 8601) |
| `startDate` | query | Non | — | Date de début (ISO 8601) |
| `statuses` | query | Non | — | Statuts (séparés par des virgules) |



## Réponses

| Statut | Description |
| --- | --- |
| `200` | Liste paginée des paiements |
| `401` | Clé API invalide ou manquante |

## Erreurs

Les erreurs suivent le format JSON standard (code de statut et message lisible par machine). Validez les entrées avant appel ; **401** indique une clé manquante/invalide, **404** une ressource introuvable pour cette organisation, **429** une limitation de débit. Pour les retries sûrs sur les créations, envoyez une clé d’idempotence quand votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/beneficiary-payouts?offset=1&limit=1" \
  -H "X-API-KEY: $LOMI_API_KEY"
```

## OpenAPI

- **operationId**: `BeneficiaryPayoutsController_findAll`
- **Operation**: `GET /beneficiary-payouts`

Schémas complets et **Try it** : [référence API](/api/index). Contrat machine : `openapi.json` à la racine des docs.
