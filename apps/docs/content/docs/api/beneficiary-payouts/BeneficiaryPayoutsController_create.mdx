---
title: "Create beneficiary payout"
description: "Create beneficiary payout"
full: true
method: post
path: /beneficiary-payouts
operationId: BeneficiaryPayoutsController_create
---

## Overview

Create beneficiary payout

Initiates a payout from your balance to a third-party beneficiary on supported rails.


### When to use this

Use when paying suppliers or partners to a saved beneficiary profile—not the same as a standard organization payout.

### Good to know

Processing is **asynchronous**. Treat the response as accepted for processing; definitive status arrives via [outbound webhooks](/api/webhooks/WebhooksController_findAll) and reconciliation APIs.

### See also

[List beneficiary payouts](/api/beneficiary-payouts/BeneficiaryPayoutsController_findAll) · [Check balance](/api/accounts/AccountsController_checkAvailableBalance)


## Authentication

Merchant routes require an API key in the `X-API-KEY` header (see [Integration overview](/reference/setup/overview)). Use a **test** key against `https://sandbox.api.lomi.africa` and a **live** key against `https://api.lomi.africa`.

## Endpoint

`POST /beneficiary-payouts`

Base URLs:

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Request

### Path parameters

_No path parameters beyond the URL pattern._


### Query parameters

_No query parameters._


### Request body

_No application/json body for this operation._


## Responses

| Status | Description |
| --- | --- |
| `201` | Paiement initié avec succès (statut en attente) |
| `400` | Entrée invalide ou solde insuffisant |
| `401` | Clé API invalide ou manquante |

## Errors

Errors follow the standard JSON error format (status code and machine-readable message). Validate inputs before calling; **401** indicates a missing/invalid key, **404** a missing resource for this organization, **429** rate limiting. For safe retries on create-style calls, send an idempotency key when your flow supports it.

## Example

```bash
curl -sS -X POST "https://sandbox.api.lomi.africa/beneficiary-payouts" \
  -H "X-API-KEY: $LOMI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

## OpenAPI

- **operationId**: `BeneficiaryPayoutsController_create`
- **Operation**: `POST /beneficiary-payouts`

Full schemas and **Try it**: [API reference](/api/index). Machine-readable contract: repo `apps/docs/openapi.json`.
