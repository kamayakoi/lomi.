---
title: "List customer subscriptions"
description: "List customer subscriptions"
full: true
method: get
path: /subscriptions/customer/{customerId}
operationId: SubscriptionsController_findByCustomer
---

## Overview

List customer subscriptions

Returns subscriptions for a given customer. Responds with 404 if the customer does not exist or is not accessible for this API key.


## Authentication

Merchant routes require an API key in the `X-API-KEY` header (see [Integration overview](/reference/setup/overview)). Use a **test** key against `https://sandbox.api.lomi.africa` and a **live** key against `https://api.lomi.africa`.

## Endpoint

`GET /subscriptions/customer/{customerId}`

Base URLs:

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Request

### Path parameters

| Name | Required | Schema | Description |
| --- | --- | --- | --- |
| `customerId` | Yes | — | Customer UUID |

### Query parameters

_No query parameters._




## Responses

| Status | Description |
| --- | --- |
| `200` | Customer subscriptions list |
| `401` | API key missing or invalid |
| `404` | Customer not found or access denied |

## Errors

Errors follow the standard JSON error format (status code and machine-readable message). Validate inputs before calling; **401** indicates a missing/invalid key, **404** a missing resource for this organization, **429** rate limiting. For safe retries on create-style calls, send an idempotency key when your flow supports it.

## Example

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/subscriptions/customer/value" \
  -H "X-API-KEY: $LOMI_API_KEY"
```

## OpenAPI

- **operationId**: `SubscriptionsController_findByCustomer`
- **Operation**: `GET /subscriptions/customer/{customerId}`

Full schemas and **Try it**: [API reference](/api/index). Machine-readable contract: repo `apps/docs/openapi.json`.
