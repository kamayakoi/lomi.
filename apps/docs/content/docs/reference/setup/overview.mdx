---
title: 'Overview'
description: "Welcome to the lomi.'s REST API documentation. This API allows you to integrate lomi.'s payment capabilities directly into your applications."
---

**New to the API?** Start with the **[API integration](/reference/setup/integration)** guide (first request, API index, Try it, SDKs, and webhooks)—then return here for the full resource map.

You can use the API to manage resources like payment providers and configure webhooks to receive real-time event notifications.

Machine-readable paths, methods, and security are published in the [API reference](/api/index) (regenerated from the same Nest controllers as production).

## Environments and authentication

lomi. provides separate **Test** and **Live** environments, each with its own base URL and API keys. All API requests must be authenticated using your secret API key passed in the `` `X-API-Key` `` header.

For full details on environments, base URLs, obtaining API keys, and security best practices, see the [Authentication](/reference/setup/authentication) guide.

To simulate payments in test mode (cards, Wave, MTN MoMo, test balances), see **[Sandbox payments](/reference/setup/sandbox-payments)**.

## Requests and responses

All API requests should be made over HTTPS to the appropriate base URL. Request bodies (for `` `POST` ``, `` `PATCH` `` methods) should be sent as JSON.

Responses are returned in JSON format. Successful requests return `` `2xx` `` status codes. Failed requests return `` `4xx` `` or `` `5xx` `` status codes with a standardized error object in the response body.

Learn more about the structure of success and error responses in the [Errors](/reference/reference/errors) guide.

## Payment behavior (beyond endpoints)

- **[Payment state machine](/reference/reference/payment-state-machine)** — transaction/provider status transitions and completion idempotency.
- **[Balance and settlement](/reference/platform/balance-and-settlement)** — when balances update and availability delays.
- **[Checkout behavior](/reference/payments/checkout-behavior)** — sessions, links, coupons, and checkout-wide rules.
- **[Payout lifecycle](/reference/payments/payout-lifecycle)** — withdrawal and payout completion semantics.
- **[Webhook reliability](/core/advanced-guides/webhook-reliability)** — retries and idempotent handling.

## API resources

This core reference section covers the following API resources:

- **[Authentication](/reference/setup/authentication):** How to authenticate your API requests.
- **[Errors](/reference/reference/errors):** Understanding API error responses.
- **[Data Models](/reference/reference/data-models):** Structure of key API objects.
- **[Merchants](/reference/platform/merchants):** Managing merchant account details and metrics.
- **[Customers](/reference/platform/customers):** Creating and managing customer profiles.
- **[Products](/reference/payments/products):** Managing product definitions.
- **[Transactions](/reference/payments/transactions):** Retrieving payment transaction details.
- **[Subscriptions](/reference/payments/subscriptions):** Listing and retrieving subscription instances (and related SDK helpers).
- **[Customer Subscriptions](/reference/platform/customer-subscriptions):** Managing individual customer subscriptions.
- **[Checkout Sessions](/reference/payments/checkout-sessions):** Creating sessions for online checkouts.
- **[Payment Links](/reference/payments/payment-links):** Creating shareable payment pages.
- **[Providers](/reference/reference/data-models#provider-object):** Retrieving connected payment provider information.
- **[Webhooks](/reference/payments/webhooks):** Managing webhook endpoints for event notifications.
- **[Refunds](/reference/payments/refunds):** Retrieving refund details (Beta).
- **[Changelog](/reference/changelog/changelog):** Notable API changes and updates.

## Versioning

The API is versioned in documentation and client libraries (for example the OpenAPI **info.version** field), but **there is no `/v1` prefix** on HTTP paths. Breaking changes are communicated through the [Changelog](/reference/changelog/changelog) and migration notes; we avoid shipping incompatible changes without notice.
