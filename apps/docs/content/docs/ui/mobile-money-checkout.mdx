---
title: Mobile Money Checkout
description: A specialized checkout card form for Mobile Money providers.
---

The `MobileMoneyCheckoutCard` is a standalone, copy-pasteable UI component built with standard HTML elements and Tailwind CSS semantic utility classes. It adapts automatically to your theme without requiring additional Shadcn primitives.

## Preview

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <MobileMoneyCheckoutCard 
        amount={4900} 
        currency="XOF" 
        onSubmit={(phone) => alert(`Processing payment for ${phone}`)} 
      />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { MobileMoneyCheckoutCard } from '@/components/lomi-ui/mobile-money-checkout-card';

    export default function Demo() {
      return (
        <MobileMoneyCheckoutCard 
          amount={4900} 
          currency="XOF" 
          onSubmit={(phone) => alert(`Processing payment for ${phone}`)} 
        />
      );
    }
    ```
  </Tab>
</Tabs>
