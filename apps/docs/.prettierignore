# MDX files in content directory have special formatting requirements
# that conflict with standard prettier formatting
content/

# Other common files to ignore
pnpm-lock.yaml
node_modules/
.next/
dist/
build/
*.log