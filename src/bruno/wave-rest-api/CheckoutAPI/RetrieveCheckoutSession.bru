meta {
  name: Retrieve Checkout Session
  type: http
  seq: 4
}

get {
  url: {{base_url}}/v1/checkout/sessions/:id
  auth: bearer
}

headers {
  Authorization: Bearer {{access_token}}
}

docs {
  Retrieves a Checkout Session object.

  Parameters:
  No parameters.

  Returns:
  Returns a Checkout Session object if a valid identifier was provided. Returns a checkout session error otherwise.
}