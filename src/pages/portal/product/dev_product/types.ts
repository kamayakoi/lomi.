export interface Product {
    product_id: string
    merchant_id: string
    name: string
    description: string
    price: number
    currency_code: string
    image_url: string
    is_active: boolean
    created_at: string
    updated_at: string
}
